\ProvidesPackage{styling/yaml-listing}

\RequirePackage{listings}

\lstdefinelanguage{yaml} {
  keywords={true,false,y,n,name,cmd,allow,deny,taint,defaultTaint},
  emph={path,pathname,access,terminal,null,to,signals,random},
  sensitive=false,
  comment=[l]{\#},
  moredelim=[l]{\&},
  morestring=[b]',
  morestring=[b]",
  literate =    {---}{{\ProcessThreeDashes}}3
                {>}{{\textcolor{red}\textgreater}}1
                {|}{{\textcolor{red}\textbar}}1
                {\ -\ }{{\ -\ }}3,
}
